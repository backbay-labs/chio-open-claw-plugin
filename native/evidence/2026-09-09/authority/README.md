# Bounded real-host authority observations

No host is accepted by this record. Each passing case used the actual native host, actual OpenAI provider, current kernel, and independent readonly Docker resource and dispatch observations. Required I01-I08 cases remain open.

The aggregate budget run authorized three invocations shared across four tools. Write, edit, and read completed and were acknowledged; list received a signed kernel invocation-budget denial. Exactly three resource dispatches occurred.

The seven approval stages cover pending, missing decision, changed arguments, approved resume, completed replay, pending rejection, and rejected resume. Only approved resume performed a resource effect. Each native-dispatch.json records the actual host call, arguments and return identity. Additional attempts remain in the observations and count toward every effect assertion. Operator approval itself caused no protected dispatch.

The first approval invocation passed an image tag where a digest was required. Launch correctly refused before host execution. The repaired driver resolves and records the immutable image first: sha256:98404ca86440e10066733abc421e8af82a40571f46d244c0dec14ea99d5a6d08. This candidate revalidates changed full native-history outcomes even if a prior request identifier was already acknowledged.
