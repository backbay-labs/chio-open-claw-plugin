# Native HTTP host candidate

Status: unaccepted. The cold-installed archive and the image built by that
installed archive completed five bounded actual OpenClaw/OpenAI cases with
kernel `33dd1dea21a4`: useful write/edit/read/list, forbidden read, forbidden
write, final response loss with exact operator recovery, and substituted output.
Each case has independent resource file and dispatch observations. No required
I01-I08 gate is closed by this subset.

The useful workflow has four dispatches, four acknowledgements and the expected
edited bytes. Each policy denial has zero dispatches. A lost response leaves
one original write and no delivery acknowledgement; another actual host with
the same authority cannot replace it. Operator export/read/acknowledgement
causes no effect; a new actual host reads the original bytes afterward.
Conversation resume remains disabled; this is host restart coverage.

The predecessor accepted forged result bytes under an unchanged authentic
receipt, returned exit 0, and acknowledged that delivery. The repaired guest
verifies the signed terminal result hash and exact host request identity.
The same injected fault now returns unresolved, with no acknowledgement and
no forged text reported to the model. The resource read is retained as an
actual operation. The injection does not change resource bytes or forge a
kernel receipt. The component suite has 18 passing tests and no skips.

Prototype failures remain recorded: container ownership initialization,
unsupported host bind mount, missing model-visible tool schemas, and parallel
tool calls fenced before their predecessor was acknowledged. Prototype 5
completed useful work, but preceded the received-result repair. None of these
older artifacts is a current accepted release.

The launcher keeps the kernel credential and journal outside the guest;
Docker internal networking, read-only mounts and disabled native paths still
need their final adversarial process/host matrix. Kernel faults, full authority
and approval coverage, cancellation, later crash cutpoints, upgrades, removal
and publication remain required. Earlier native-profile and hosted-chat records
do not establish these properties for this image.
